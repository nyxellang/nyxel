#!/usr/bin/env python3
"""
nyx  —  the Nyxel command-line tool.

Usage:
  nyx run   <file.nx>  [args…]
  nyx repl
  nyx check <file.nx>
  nyx version
  nyx help
"""
import sys
from pathlib import Path

# Allow running from the project root without installing
sys.path.insert(0, str(Path(__file__).parent))

from nyxel.cli import main

if __name__ == "__main__":
    main()
